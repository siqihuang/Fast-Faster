using UnityEngine;
using System.Collections;

/*
 * the script is attached to an empty object
 * you should implement the function of buy cars, buy charaters, buy gameMoney
 * you need to read the existing resource information into the script
 * you also need to write the new car, money, and character information to the file
 */ 
public class storeUI : MonoBehaviour {
	private readFile read;
	private writeFile write;
	private string fileName;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	void purchaseCars(){
	}

	void purchaseCharacters(){
	}

	void buyGameMoney(){
	}
}
